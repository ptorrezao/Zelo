# @zelo/api-client

**Gerado, não escrito à mão.** Os tipos saem do OpenAPI que a `Zelo.Api`
produz. Alterar um DTO no backend parte o build do frontend no mesmo
commit — é esse o objetivo.

```bash
pnpm gen   # ver tools/scripts/gen-api-client.sh
```
